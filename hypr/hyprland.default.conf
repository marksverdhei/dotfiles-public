# Learn how to configure Hyprland: https://wiki.hyprland.org/Configuring/

# Use defaults Omarchy defaults (but don't edit these directly!)
source = ~/.local/share/omarchy/default/hypr/autostart.conf
source = ~/.local/share/omarchy/default/hypr/bindings/media.conf
source = ~/.local/share/omarchy/default/hypr/bindings/clipboard.conf
source = ~/.local/share/omarchy/default/hypr/bindings/tiling-v2.conf
source = ~/.local/share/omarchy/default/hypr/bindings/utilities.conf
source = ~/.local/share/omarchy/default/hypr/envs.conf
source = ~/.local/share/omarchy/default/hypr/looknfeel.conf
source = ~/.local/share/omarchy/default/hypr/input.conf
source = ~/.local/share/omarchy/default/hypr/windows.conf
source = ~/.config/omarchy/current/theme/hyprland.conf

# Change your own setup in these files (and overwrite any settings from defaults!)
source = ~/.config/hypr/input.conf
source = ~/.config/hypr/bindings.conf
source = ~/.config/hypr/autostart.conf

# Machine-specific overrides (monitors, GPU settings, env vars, vibe mode, etc.)
# These are symlinked by the private bootstrap to the correct machine config
source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/machine.conf

# Dwindle layout (natural splitting)
general {
    layout = dwindle
    
    # Custom border colors (change these rgba values to customize)
    col.active_border = rgba(d994d9ee) rgba(3294d9ee) rgba(d994d9ee) 45deg
    col.inactive_border = rgba(595959aa)
}

animations {
    animation = borderangle, 1, 30, linear, loop
}

dwindle {
    preserve_split = true
}

# hyprwinwrap - run any app as wallpaper
# plugin = ~/.local/share/hyprland/plugins/libhyprwinwrap.so
exec-once = hyprpm reload -n
plugin {
    hyprwinwrap {
        class = wallpaper
    }
    
    liquid-glass {
        blur_strength = 0.5
        refraction_strength = 0.02
        chromatic_aberration = 0.003
        fresnel_strength = 0.25
        specular_strength = 0.15
        edge_thickness = 0.03
    }
}

# Vibe shader windows - full opacity so they're visible
windowrule = opacity 1.0 1.0, match:title ^(vibe.*)$

# Live subtitles overlay — click-through on transparent areas
layerrule = ignore_alpha 0.0, match:namespace eww-live-subs

# hypr3d - 3D effects plugin (disabled - uncomment to enable)
# plugin = ~/.local/share/hyprland/plugins/libhypr3d.so
#
# plugin {
#     hypr3d {
#         enabled = true
#         perspective {
#             enabled = true
#             depth_scale = 0.65
#             max_depth = 4
#             opacity_falloff = 0.25
#         }
#         cube {
#             enabled = true
#             duration_ms = 500
#             fov = 70.0
#             zoom_out = 0.7
#         }
#         window_anim {
#             enabled = true
#             open_style = flip
#             close_style = shrinkrot
#         }
#         dof {
#             enabled = true
#             unfocus_blur_max = 6
#         }
#         overview {
#             enabled = true
#             columns = 4
#             perspective_tilt = 15.0
#         }
#     }
# }
#
# bind = SUPER, TAB, hypr3d:overview_toggle,
# bind = SUPER CTRL, Right, hypr3d:cube_next,
# bind = SUPER CTRL, Left, hypr3d:cube_prev,

decoration {
    rounding = 10
}
